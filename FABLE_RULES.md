# 페이블(Fable) 사용규칙

이 문서는 이 프로젝트에서 Claude 모델별 역할 분담 규칙을 정의한다.
모든 개발 작업은 아래 4단계 파이프라인을 따른다.

## 역할 분담

| 단계 | 담당 모델 | 실행 방식 | 역할 |
|------|-----------|-----------|------|
| 1. 계획 및 설계 | **Fable** | 메인 세션 | 요구사항 분석, 구현 계획 수립, 상세 설계 작성 |
| 2. 구현 | **Sonnet** | 서브에이전트 (`model: sonnet`) | Fable의 설계대로 코드 작성. 설계에서 벗어나는 임의 판단 금지 |
| 3. 1차 검수 | **Opus** | 서브에이전트 (`model: opus`) | Sonnet의 구현 결과물을 설계 기준으로 검수 (버그, 설계 위반, 누락) |
| 4. 최종 검수 | **Fable** | 메인 세션 | Opus의 검수 결과를 반영·판단하고 최종 승인 |

## 단계별 상세 규칙

### 1단계 — 계획 및 설계 (Fable)
- 요구사항을 분석하고 관련 코드를 파악한 뒤 구현 계획을 세운다.
- 설계에는 다음이 포함되어야 한다:
  - 수정/생성할 파일 목록
  - 각 파일에서 해야 할 작업의 구체적 내용
  - 제약 조건 및 주의 사항 (기존 코드 컨벤션, 사이드 이펙트 등)
- 설계는 Sonnet이 추가 질문 없이 그대로 구현할 수 있을 만큼 구체적이어야 한다.

### 2단계 — 구현 (Sonnet)
- Fable이 작성한 설계를 프롬프트로 전달받아 그대로 구현한다.
- 설계에 명시되지 않은 구조 변경, 리팩터링, 기능 추가를 하지 않는다.
- 설계와 실제 코드가 충돌해 구현이 불가능하면, 임의로 우회하지 말고 그 사실을 결과에 보고한다.

### 3단계 — 1차 검수 (Opus)
- Sonnet의 변경 사항을 설계 문서와 대조하여 검수한다.
- 검수 항목:
  - 설계 준수 여부 (누락된 항목, 설계에 없는 임의 변경)
  - 버그 및 논리 오류
  - 기존 코드와의 정합성 (컨벤션, 사이드 이펙트)
- 발견한 문제를 파일/라인 단위로 구체적으로 보고한다.

### 4단계 — 최종 검수 (Fable)
- Opus의 검수 결과를 검토하고, 유효한 지적은 수정을 지시하거나 직접 수정한다.
- 수정이 크면 2~3단계를 다시 거친다. 경미하면 Fable이 직접 고친다.
- 최종 승인 후 사용자에게 결과를 요약 보고한다.

## 작업 로그 (필수)

- 모든 작업이 끝나면 [WORKLOG.md](WORKLOG.md)에 기록을 남긴다.
- 서식: `## 날짜 - 작업내용` 헤더 아래에 변경 사항을 항목별로 정리한다.
- 최신 항목이 위로 오도록 추가한다.

예시:

```markdown
## 2026-07-30 - 잭팟 연출 버그 수정

- `JackpotEffect.cs`: 코인 연출 중복 재생 수정
- `SlotManager.cs`: 릴 정지 타이밍 조정
```

## 적용 범위

- **적용**: 코드 작성·수정·리팩터링 등 실제 구현이 있는 모든 작업
- **제외**: 단순 질문/답변, 코드 조회·설명, 문서 열람 등 구현이 없는 작업은 Fable이 메인 세션에서 직접 처리한다.
